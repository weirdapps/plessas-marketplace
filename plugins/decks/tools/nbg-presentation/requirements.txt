# NBG presentation tools: nbg_build.py, nbg_validate.py, nbg_render.py,
# nbg_extract.py, nbg_record.py and the two injectors.
# bin/decks-py installs this file into its own environment on first use; nothing
# else needs to. Every package below publishes wheels for macOS, Linux and Windows.

# Deck specs and the brand tokens
pyyaml>=6.0.3
# The deck spec contract (deck.schema.json is JSON Schema 2020-12)
jsonschema>=4.26.0

# Building and reading .pptx
python-pptx>=1.0.2
lxml>=6.1.3
# Text measurement with the Aptos files, image sizes, PNG pages
Pillow>=12.3.0
# SVG icons and infographics rasterised for the image element (MIT)
resvg-py>=0.5.0

# Safe XML parsing for validation (nbg_validate.py)
defusedxml>=0.7.1

# render: PDF pages to PNG (BSD-3-Clause / Apache-2.0) and the embedded-font check,
# plus PDF text for extract (BSD-3-Clause)
pypdfium2>=5.13.0
pypdf>=6.19.0
